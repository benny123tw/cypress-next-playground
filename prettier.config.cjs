/** @type {import('prettier').Options}  */
const config = {
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  printWidth: 100,
  plugins: ['prettier-plugin-tailwindcss'],
}

module.exports = config
